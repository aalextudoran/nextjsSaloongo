const nextConfig = {
//   webpack(config, { dev }) {
//     if (dev) {
//       config.devtool = 'source-map';
//     }
//     return config;
//   },
  productionBrowserSourceMaps: true,
};

module.exports = nextConfig;