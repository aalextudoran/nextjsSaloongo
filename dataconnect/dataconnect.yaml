specVersion: "v1beta"
serviceId: "saloongo-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "saloongo-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
